import { AmountvalidatorDirective } from './amountvalidator.directive';

describe('AmountvalidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AmountvalidatorDirective();
    expect(directive).toBeTruthy();
  });
});
